Fast Rust-based linter and formatter from [Biome](https://biomejs.dev):

```sh
{{run "lint"}}     # check for lint errors
{{run "format"}}   # rewrite files in place
{{run "check"}}    # lint + format + import sorting in one pass
```

Config lives at `biome.json` at the repo root.

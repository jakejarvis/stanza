Fast Rust-based linter and formatter from [Oxc](https://oxc.rs):

```sh
{{run "lint"}}       # check for lint errors
{{run "lint:fix"}}   # autofix what's fixable
{{run "format"}}     # rewrite files in place
```

Config lives at `.oxlintrc.json` and `.oxfmtrc.json` at the repo root.
